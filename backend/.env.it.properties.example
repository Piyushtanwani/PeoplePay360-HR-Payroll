# Copy to .env.it.properties and fill in. Used only by `mvn verify -Pit`.
# The database named in IT_DB_URL is CLEANED before every run: never point it at development data.
spring.datasource.username=your_postgres_role
spring.datasource.password=your_postgres_password
